# API key from https://aistudio.google.com/apikey
GEMINI_API_KEY=your_api_key_here

# Optional — override the default model (e.g. if your account doesn't have
# access to the current default, or a model is deprecated for new keys).
# GEMINI_MODEL=gemini-flash-latest
